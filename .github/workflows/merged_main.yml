name: Merged main branch
on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Tokyo
    steps:
      - run: |
          echo "run test"
          echo "${{ github.ref }}"
          echo "${{ github.event.pull_request.merged }}"

  action:
    runs-on: ubuntu-latest
    needs: test
    if: >
      github.ref == 'main'
    steps:
      - run: |
          echo "run action"
